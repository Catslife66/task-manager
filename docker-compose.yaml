services:
  api:
    build:
      context: ./backend-api
      dockerfile: Dockerfile
    container_name: task_manager_app
    env_file:
      - ./backend-api/.env
    ports:
      - "8000:8000"
    volumes:
      - ./backend-api:/app
    depends_on:
      - db

  db:
    image: postgres:latest
    container_name: task_manager_db
    env_file:
      - ./backend-api/.env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend-api/docker/initdb:/docker-entrypoint-initdb.d

  # web:
  #   build:
  #     context: ./frontend-web
  #     dockerfile: Dockerfile
  #   container_name: task_manager_web
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - ./frontend-nextjs:/app
  #   depends_on:
  #     - api

volumes:
  postgres_data:
